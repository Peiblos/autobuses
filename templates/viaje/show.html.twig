{% extends 'base.html.twig' %}

{% block title %}Viaje{% endblock %}

{% block body %}
    <h1>Viaje</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ viaje.id }}</td>
            </tr>
            <tr>
                <th>Asientos</th>
                
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_viaje_index') }}">back to list</a>

    <a href="{{ path('app_viaje_edit', {'id': viaje.id}) }}">edit</a>

    {{ include('viaje/_delete_form.html.twig') }}
{% endblock %}
