{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<h1>Malsa</h1>
{% if app.user %}

    <p>Bienvenidos a Malsa {{app.user.username}}, <a href="/logout">Cerrar sesion</a></p>
    <a href="/itinerario">Itinerarios disponibles</a>
        {% if venta %}
            <h1>Venta completada</h1>
        {% endif %}

{% else %}

    <p>Bienvenidos a Malsa, Por favor <a href="/login">Inicia sesion</a> o <a href="/register">Registrate</a></p>
    
{% endif %}

{% if is_granted("ROLE_ADMIN") %}
<hr/>
<h1>PANEL DE ADMINISTRACIÓN</h1>
    <a href="{{ path('app_itinerario_new') }}">Crear Nuevo itinerario</a><br>
    <a href="{{ path('app_autobus_new') }}">Crear Nuevo Bus</a><br>
    <a href="{{ path('app_horario_new') }}">Crear Nuevo Horario</a><br>
    <a href="{{ path('app_viaje_new') }}">Crear Nuevo Viaje</a>
{% endif %}

{% endblock %}
